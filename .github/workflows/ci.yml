name: CI — Layer Byte-compile

on:
  push:
    branches: [master]
    paths:
      - 'what/standard/layers/**/*.el'
  pull_request:
    paths:
      - 'what/standard/layers/**/*.el'

jobs:
  byte-compile:
    name: Byte-compile (Emacs ${{ matrix.emacs-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        emacs-version:
          - '28.2'
          - '29.4'
          - 'snapshot'

    steps:
      - uses: actions/checkout@v4

      - name: Install Emacs ${{ matrix.emacs-version }}
        uses: purcell/setup-emacs@master
        with:
          version: ${{ matrix.emacs-version }}

      - name: Byte-compile all layer elisp files
        run: |
          find what/standard/layers -name '*.el' -print0 \
            | xargs -0 emacs --batch -f batch-byte-compile

  validate:
    name: Layer structure validation (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run validate_layers.py
        run: python3 what/standard/index/validate_layers.py
