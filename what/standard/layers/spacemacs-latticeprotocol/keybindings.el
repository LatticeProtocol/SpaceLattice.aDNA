;;; keybindings.el --- spacemacs-latticeprotocol distribution layer keybindings  -*- lexical-binding: t -*-
;;
;; Author:  LatticeProtocol / Daedalus (Spacemacs.aDNA)
;; License: GPL-3.0
;;
;; `SPC o l' LP prefix bindings — populated by mission P4-02 from the
;; P3-08 binding table in `who/operators/stanley.md §3.5'.
;;
;; P3-08 binding table (source of truth):
;;   SPC o l h  →  adna-home
;;   SPC o l f  →  adna-find-context
;;   SPC o l s  →  adna-session-new
;;   SPC o l g  →  adna-graph
;;   SPC o l c  →  claude-code-ide buffer

;; Populated in P4-02.

;;; keybindings.el ends here
