;;; layers.el --- spacemacs-latticeprotocol distribution layer dependencies  -*- lexical-binding: t -*-
;;
;; Author:  LatticeProtocol / Daedalus (Spacemacs.aDNA)
;; License: GPL-3.0
;;
;; Declares the upstream spacemacs distribution as a layer dependency so
;; Spacemacs loads the standard distribution before the LP overrides.

(configuration-layer/declare-layer-dependencies '(spacemacs))

;;; layers.el ends here
